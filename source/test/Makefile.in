##
## File:	$URL: file:///usr/casc/samrai/repository/SAMRAI/tags/v-2-2-0/source/test/Makefile.in $
## Package:	SAMRAI test
## Copyright:	(c) 1997-2007 Lawrence Livermore National Security, LLC
## Revision:	$LastChangedRevision: 1768 $
## Modified:	$LastChangedDate: 2007-12-11 16:02:04 -0800 (Tue, 11 Dec 2007) $
## Description:	makefile for testing parts of SAMRAI library
##

SAMRAI	      = @top_srcdir@
SRCDIR        = @srcdir@
OBJECT        = ../..
SUBDIR        = source/test

SUBDIRS = async_comm \
	applications boundary communication clustering dataops dataaccess \
	dlbg emb_bdry FAC hierarchy hypre ieee inputdb HDF5 locally_active \
	mblkcomm MblkLinAdv nonlinear patchbdrysum patch_configuration pointer \
        sundials schedules timers variables vector

default: check	

include $(OBJECT)/config/Makefile.config

library:
lib1d:
lib2d:
lib3d:
libXd:

check:
	for DIR in $(SUBDIRS); do (cd $$DIR && $(MAKE) $@); done

check2d:
	for DIR in $(SUBDIRS); do (cd $$DIR && $(MAKE) $@); done

check3d:
	for DIR in $(SUBDIRS); do (cd $$DIR && $(MAKE) $@); done

checktest:
	for DIR in $(SUBDIRS); do (cd $$DIR && $(MAKE) $@); done

clean:
	for DIR in $(SUBDIRS); do (cd $$DIR && $(MAKE) $@); done
